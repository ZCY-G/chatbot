# AI 聊天机器人

## 系统要求

1. Node.js 18.18 及以上
2. pnpm 10 及以上

## 开始

1. 运行 `pnpm install` 安装依赖。
2. 运行 `pnpm build` 编译项目。
3. 运行 `pnpm start` 启动项目。
4. 在浏览器中打开 http://localhost:3000/

## 修改代码

1. 运行 `pnpm install` 安装依赖。
2. 运行 `pnpm dev` 调试项目。
